<?xml version="1.0" encoding="utf-8"?>
<!--
  Clowder MSIX manifest template. pack-msix.ps1 substitutes the @TOKEN@ placeholders.
  - Store submission: replace Identity Name / Publisher / PublisherDisplayName with the values
    reserved in Partner Center.
  - Local test (self-signed): Publisher must match the signing cert Subject (the script handles it).
  - Full-trust Win32 app: EntryPoint=Windows.FullTrustApplication + rescap:runFullTrust — required so the
    app keeps unrestricted std::fs access (the full file-system explorer) and can spawn shells/ConPTY,
    which an AppContainer would block.
-->
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
  IgnorableNamespaces="uap rescap">

  <Identity Name="@IDENTITY_NAME@"
            Publisher="@PUBLISHER@"
            Version="@VERSION@"
            ProcessorArchitecture="x64" />

  <Properties>
    <DisplayName>Clowder</DisplayName>
    <PublisherDisplayName>@PUBLISHER_DISPLAY@</PublisherDisplayName>
    <Logo>Assets\StoreLogo.png</Logo>
  </Properties>

  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.17763.0" MaxVersionTested="10.0.26100.0" />
  </Dependencies>

  <Resources>
    <Resource Language="ko-KR" />
    <Resource Language="en-US" />
  </Resources>

  <Applications>
    <Application Id="Clowder" Executable="@EXE@" EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
        DisplayName="Clowder"
        Description="여러 Claude Code 세션을 한 화면에서 다루는 터미널 워크스페이스 — 터미널·git·탐색기·문서 뷰어 · 오프라인 / A terminal workspace for parallel Claude Code sessions · offline"
        BackgroundColor="transparent"
        Square150x150Logo="Assets\Square150x150Logo.png"
        Square44x44Logo="Assets\Square44x44Logo.png">
        <uap:DefaultTile Square71x71Logo="Assets\Square71x71Logo.png" />
      </uap:VisualElements>
    </Application>
  </Applications>

  <Capabilities>
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>
</Package>
